package net.rim.device.api.system;

public class Device {
    // Since BlackBerry API 3.6.0; controlled (signed-only) on device.
    public static boolean setDateTime(long dateTimeMillis) {
        return false;
    }
}
